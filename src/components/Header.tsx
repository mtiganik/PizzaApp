import React from "react";
import {Text,View} from 'react-native';


const Header = () => {
  return(
    <View style={{alignItems:'center'}}>
      <Text style={{fontSize:42}}>Pizza Calculator</Text>
    </View>
  )
}

export default Header;